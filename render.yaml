services:
  - type: web
    name: sw2627-data-product-development-campaign-canvas
    env: python
    buildCommand: pip install -r requirements.txt
    startCommand: streamlit run src/app.py --server.port $PORT --server.address 0.0.0.0
